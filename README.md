# random-fast

This module exports a function that calculates a random number as fast as possible.

The generator function is based on the assumption that we have no free will and every thing is pre-determined.

The generator function returns a number that I pre-determined, randomly.
